# Contributing

- **JavaScript/TypeScript**: This repo uses [Bun](https://bun.sh) for dependency management and scripts. Use `bun install`, `bun run`, `bun add`, and `bunx` (not npm/npx/yarn/pnpm). Each app under `apps/` has its own `package.json`; run commands from the app directory.
