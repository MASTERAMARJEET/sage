{
  "name": "sage-brain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "d1:migrate:local": "wrangler d1 migrations apply sage-brain-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply sage-brain-db --remote"
  },
  "dependencies": {
    "agents": "^0.0.98",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260217.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.33.0"
  }
}
